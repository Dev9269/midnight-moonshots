{
  "name": "private-pay",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "description": "PrivatePay - private payroll & splits on Midnight (Compact contract + dependency-free toolchain)",
  "scripts": {
    "compile": "compact compile contracts/private_pay.compact contracts/managed/private_pay",
    "test": "node --test \"tests/*.test.js\"",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "demo": "node src/index.mjs",
    "dev": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}